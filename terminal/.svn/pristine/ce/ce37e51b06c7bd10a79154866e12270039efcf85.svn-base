package com.asiainfo.cmc.test.spirngJunitTest.modelTest;

import java.util.HashMap;
import java.util.Map;

import org.apache.ibatis.session.SqlSession;
import org.junit.Test;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.test.annotation.Rollback;
import org.springframework.transaction.annotation.Transactional;

import com.asiainfo.cmc.service.model.eightCharacters.IAmonutService;

import com.asiainfo.cmc.vo.model.EcharDataVo;

/*public class AmountService extends BaseJunit4Test {
	
	
	@Autowired//自动注入
	private IAmonutService iAmonutService;
	
	@Autowired//自动注入
	private  SqlSession sqlSession;
	
	@Test
	@Transactional   //标明此方法需使用事务  
    @Rollback(false)  //标明使用完此方法后事务不回滚,true时为回滚 
	public void test(){
		System.out.println("测试Spring整合Junit4进行单元测试");
		Map<String,String> param = new HashMap<String,String>();
		param.put("dim_key", "four_echelon_sales_company");
		param.put("parent_dim","手机");
		param.put("month_value","201806");
		EcharDataVo echarDataVo = iAmonutService.getEcharDataVo(param);
		System.out.println("***"+echarDataVo.getData());
		System.out.println("***"+echarDataVo.getNest_data());
		
		 
	}
}*/
